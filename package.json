{
  "name": "relvo",
  "version": "0.1.0",
  "private": true,
  "description": "Assistant IA de pilotage des sollicitations professionnelles",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "typecheck": "pnpm --filter web typecheck && pnpm --filter @relvo/db typecheck",
    "test": "pnpm --filter @relvo/db test && pnpm --filter @relvo/storage test && pnpm --filter web test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "changelog": "node scripts/generate-changelog.mjs",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:generate": "pnpm --filter @relvo/db generate",
    "db:migrate": "pnpm --filter @relvo/db migrate",
    "db:status": "pnpm --filter @relvo/db status",
    "db:seed": "pnpm --filter @relvo/db seed",
    "db:seed:ensure": "pnpm --filter @relvo/db seed:ensure",
    "db:reset": "pnpm --filter @relvo/db migrate:reset",
    "db:studio": "pnpm --filter @relvo/db studio",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs,json,css}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0"
  }
}
