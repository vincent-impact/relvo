# Connexion PostgreSQL utilisée par Prisma (CLI migrate/generate/studio).
# En local : pointe vers le conteneur Docker Compose (docker compose up -d).
# En prod : remplacer par l'URL Neon.
DATABASE_URL="postgresql://relvo:relvo@localhost:5432/relvo?schema=public"
