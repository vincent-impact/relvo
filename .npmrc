# pnpm — autoriser les scripts de build des deps qui en ont besoin (prisma, esbuild…)
auto-install-peers=true
strict-peer-dependencies=false
# Hoist Prisma pour qu'il soit résolu uniformément web ↔ worker
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*
