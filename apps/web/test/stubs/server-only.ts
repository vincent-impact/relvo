// Stub de `server-only` pour vitest (PITFALLS.md #34) : le paquet lève à
// l'import quand le résolveur retient sa condition navigateur — ce que fait
// l'outil de test. Vide à dessein ; aliasé dans vitest.config.ts UNIQUEMENT.
export {};
